{{#if this.cortexExtraInstalled}}
  {{#if this.char.cortex}}
    {{#if this.char.cortex.show_sheet}}
      <li class="nav-item"><a data-bs-toggle="tab" class="nav-link" href="#systemcortex">Cortex</a></li>
    {{/if}}
  {{/if}}
{{/if}}
